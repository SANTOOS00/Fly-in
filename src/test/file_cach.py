
                    # if result.node1 not in [h.name for h in self.hubs] and result.node1 != self.start_hub.name:
                    #     raise ParsingError(f"Line {parser.line_number}: Node '{result.node1}' in connection is not defined in the hubs.")
                    # if result.node2 not in [h.name for h in self.hubs] and result.node2 != self.start_hub.name:
                    #     raise ParsingError(f"Line {parser.line_number}: Node '{result.node2}' in connection is not defined in the hubs.") 